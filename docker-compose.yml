
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    container_name: backend-container

  frontend:
    build: ./frontend/frontend
    ports:
      - "3000:3000"
    container_name: frontend-container
    depends_on:
      - backend